{
  "name": "top_colors",
  "version": "0.0.1",
  "description": "get the top 'n' colors in a given image. currently supports pngs. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "topcolors": "./get_colors.js"
  },
  "author": "Andrew Bloese",
  "license": "ISC",
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
